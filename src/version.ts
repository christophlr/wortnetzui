// IMPORTANT: Bump VERSION with every commit. Format: "0.XX" where XX = commit number.
// Current commit count is tracked in CLAUDE.md.
export const VERSION = "0.53";

declare const __BUILD_DATE__: string;
export const BUILD_DATE = __BUILD_DATE__;
